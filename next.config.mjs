/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['github.com'], // Allow images from github.com
      },
};

export default nextConfig;

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
//   images: {
//     domains: ['github.com'], // Add any other domains you might need
//   },
// };

// module.exports = nextConfig;
  
